<div class="product-info">

    <h2><%= product.nombre %></h2>

    <p class="text-muted">
        Marca: <strong><%= product.marca?.nombre || 'N/A' %></strong>
    </p>

    <p>
        Categoría: <strong><%= product.categoria?.nombre || 'N/A' %></strong>
    </p>

    <h3 class="product-price">$ <%= product.precio %></h3>

    <p>
        <%= product.descripcion || 'Sin descripción disponible' %>
    </p>

    <ul class="list-group mb-3">
        <li class="list-group-item">
            Stock disponible: <strong><%= product.stock %></strong>
        </li>
    </ul>

    <div class="mt-3">

     <button 
         class="btn-primary-action add-to-cart-btn"
         data-id="<%= product._id %>">
         🛒 Agregar
     </button>

    </div>

</div>